# Gomel-Flow

**Low-code конструктор для малого бизнеса Беларуси**

Платформа позволяет владельцам магазинов, кафе и сервисов без навыков программирования создавать страницы с онлайн-заказом, каталогом товаров и формой обратной связи за несколько минут.

## Возможности

- **Визуальный конструктор** — перетаскивание блоков, настройка свойств
- **6 типов блоков**: Шапка, Каталог, Контакты, Кнопка, Форма заказа, Подвал
- **Режим превью** — просмотр результата до публикации
- **Мультипроекты** — несколько проектов с переключением
- **Сохранение** — в браузере, JSON-файл, загрузка из файла
- **Шаринг** — копирование ссылки для публикации (конфиг в URL)
- **Экспорт в HTML** — скачивание готового сайта для размещения на любом хостинге
- **Форма заказа** — отправка в Telegram или Email
- **Кнопки** — ссылки на телефон, почту, мессенджеры

## Технологии

- Next.js 16, React 19
- TypeScript
- Tailwind CSS
- @dnd-kit (drag-and-drop)

## Запуск

```bash
npm install
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## Сборка

```bash
npm run build
npm start
```

## Структура проекта

```
src/
├── app/           # Страницы Next.js
│   ├── page.tsx   # Конструктор
│   └── view/      # Публичная страница (/view, /view?data=...)
├── components/constructor/
│   ├── BlockPalette.tsx
│   ├── Canvas.tsx
│   ├── PropertiesPanel.tsx
│   ├── BlockRenderer.tsx
│   ├── ConfigToolbar.tsx
│   ├── PreviewView.tsx
│   └── ProjectsModal.tsx
├── lib/           # Утилиты
└── types/         # TypeScript типы
```

## Как пользоваться

1. Перетащите блоки из палитры слева на холст
2. Выберите блок и настройте свойства справа
3. Сохраните проект (💾) или создайте новый (📂 → Новый проект)
4. Нажмите Превью для просмотра
5. Нажмите **«Скачать сайт»** — получите HTML-файл для размещения на хостинге
6. Или скопируйте ссылку для быстрого просмотра

---

**Gomel-Flow** — проект для цифровизации малого бизнеса в Беларуси.
